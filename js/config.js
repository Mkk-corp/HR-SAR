'use strict';

/**
 * App configuration — change API_BASE here when the domain changes.
 * This is the only file you need to edit to point the app at a different backend.
 */
const APP_CONFIG = {
    API_BASE: 'https://hr-sar-production.up.railway.app/api',
};
